-- 
-- 用来访问游戏配置的表
-- 
local DataTables = {}

function DataTables:lookup(table_name, column_name, id)
  -- 找到表
  -- local tables = {}
  -- 根据id找到对应row, 如果没有id使用其他属性
  -- local row = tables[id]
  -- 根据column_name获取对应属性
  -- return row[column_name]
end

return DataTables